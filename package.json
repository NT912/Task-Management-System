{
  "name": "task-management-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "concurrently -n \"BE,FE\" -c \"blue,magenta\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\""
  },
  "dependencies": {
    "zod": "^3.0.0"
  }
}